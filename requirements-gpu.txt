# Installed in `.venv-gpu` with Python 3.10.
# PyTorch CUDA packages must be installed from the official CUDA wheel index:
# python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130

opencv-python
scipy
tqdm
matplotlib
gdown
psutil
ninja
PyYAML
efficientnet-pytorch
